// Package winapi is the little of the Windows API that Runbook needs: whether
// a process is running, and when it started. It is empty everywhere else, so
// that the packages that use it can be built and read on any machine.
package winapi
